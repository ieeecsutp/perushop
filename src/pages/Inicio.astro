<!-- Sección Inicio -->
<section
  id="inicio"
  class="relative h-[90vh] flex items-center justify-center overflow-hidden">
  <!-- Fondo degradado -->
  <div class="absolute inset-0 bg-gradient-to-br"></div>

  <!-- Imagen de portada -->
  <img
    src="/portada.jpg"
    alt="Portada"
    class="absolute inset-0 w-full h-full object-cover"
  />

  <!-- Overlay oscuro más opaco para mejor contraste del texto -->
  <div class="absolute inset-0 bg-black/70"></div>

  <!-- Contenido central -->
  <div class="relative z-10 text-center px-4 max-w-4xl mx-auto">
    <!-- Título principal con negrita -->
    <h2
      class="text-3xl sm:text-4xl md:text-7xl text-primary-foreground font-serif font-bold mb-6 text-balance">
      Perú en tus manos
    </h2>

    <!-- Subtítulo / descripción -->
    <p
      class="text-lg sm:text-xl md:text-2xl text-primary-foreground mb-8 text-pretty">
      Tradición y Calidad en cada Producto
    </p>
  </div>
</section>
